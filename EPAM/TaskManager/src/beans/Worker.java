package beans;

import java.util.List;

public class Worker extends User{
    public Worker(String name, Role role, String login, String password) {
        super(name, role, login, password);
    }
}
