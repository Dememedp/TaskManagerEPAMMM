package beans;

import java.util.List;

public class Manager extends User{
    public Manager(String name, Role role, String login, String password) {
        super(name, role, login, password);
    }
}
